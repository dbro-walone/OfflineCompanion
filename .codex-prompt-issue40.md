# 修复 OfflineCompanion Issue #40 — 桌宠体验增强

你在 `fix/issue-40-tray-and-ux` 分支（基于 `rust-rewrite`）。项目是 Rust + Slint 桌面宠物，UI 在 `ui/app.slint`，逻辑在 `src-rust/main.rs`，平台代码在 `src-rust/platform.rs`，依赖在 `Cargo.toml`。所有界面文案用简体中文。请实现以下 6 点（来自 GitHub issue #40）。务必确保 `cargo check` 和 `cargo clippy` 通过（可忽略仅 Windows 平台条件编译在非 Windows 上的告警，但 Windows 目标必须可编译）。

## 背景：当前代码状态
- `rust-rewrite` 分支**没有任何系统托盘代码**，Cargo.toml 没有 `tray-icon` 依赖。
- `pet.on_exit`（src-rust/main.rs ~line 299）直接调用 `slint::quit_event_loop()`，无确认。
- `wire_notification` 的 `on_dismiss`（~line 672）和 `on_snooze`（~line 678）只隐藏通知窗口，桌宠仍停留在屏幕中央（被 `create_alert_presenter` 走到中央后未返回）。
- 右键菜单在 `ui/app.slint` 的 PetWindow 里以 `Rectangle` 居中叠在桌宠图像正上方（`x: (parent.width - self.width)/2; y: (parent.height - self.height)/2`），盖住了宠物。
- 已有 `animation_sequence: Rc<RefCell<Vec<i32>>>` 和 `frame_index` 帧动画机制，但目前只在提醒时播放 `[6,6,6,3]`。

## 需求逐条

### 1. 任务栏默认图标（可爱小图标）
- 为应用窗口设置一个默认图标。当前 `pet.window()` 和各子窗口没有设置图标。
- 请在仓库生成一个可爱的桌宠图标资源（例如用代码内嵌的简单 PNG/ICO，或一个能在构建时生成图标的方案）。优先方案：在 `build.rs` 或运行时通过 Slint/winit 设置窗口图标为一个内嵌的可爱图标（比如一个紫色的鸦影剪影或一个可爱的动物轮廓）。
- 在 Windows 上确保任务栏、标题栏、托盘使用一致的图标。

### 2. 系统托盘隐藏（关键需求）
用户原话："运行后支持 icon 可以隐藏在 windows 右边的系统托盘里面。我希望的效果是任务栏里面的图标在系统托盘里面，而不是说点击任务栏的图标的时候，小宠物会隐藏。"
- 添加 `tray-icon` crate（推荐 `tray-icon = "0.21"` 或与当前 winit/slint 兼容的版本）作为依赖。
- 在 Windows 上，应用的主窗口应当从任务栏隐藏（使用 `WS_EX_TOOL` 或通过 winit 的 skip_taskbar 等价机制），同时创建一个系统托盘图标。
- 点最小化/关闭按钮（或专门的"隐藏到托盘"入口）时，桌宠窗口隐藏，应用驻留托盘。
- 点击托盘图标可重新显示桌宠。托盘右键菜单提供"显示"和"退出"。
- 注意：Slint 1.17 + winit 0.30 环境。tray-icon crate 需要在主线程创建。请确保与 slint 事件循环兼容（tray-icon 的事件可借助 winit 的事件钩子或单独线程 + channel 转发到主线程）。如果集成复杂，可使用 Windows 原生 API（已有 `windows` crate 依赖，可加 `Win32_UI_Shell` / `Shell_NotifyIconW` / `NIN_*` 通知）作为 Windows 专用实现，非 Windows 平台用 `tray-icon` crate 或直接 stub。
- 关键：实现一个 `platform::TrayController`（或类似）抽象，提供 `show_icon` / `set_tooltip` / `on_clicked` / `on_menu_action` 回调，在 main.rs 里 wire 起来。

### 3. 提醒"知道了"之后桌宠返回原位
用户原话："任务提醒我点'知道了'之后，小宠物退回去原来的位置，不要一直在屏幕中间。"
- 当前 `create_alert_presenter`（~line 915）的 motion 只做单程 start→center。`wire_notification.on_dismiss`（~line 672）隐藏通知后桌宠留在中央。
- 修改：记录桌宠在提醒触发前的原始位置（`presenter` 闭包里已有 `start`，需把它保存到可被 dismiss 复用的状态，例如把 `home_position: Rc<RefCell<Option<(i32,i32)>>>` 传入 `create_alert_presenter` 并在 presenter 启动时写入 start，在 dismiss 时读取并启动一个返回动画）。
- 实现一个返回动画：从当前位置缓动回 home_position，时长与去程对称（受 `reduce_motion` 影响时缩短）。可复用 `PetMotion` 结构和 `motion_timer` 机制，增加一个 `direction` 或单独的"返回"闭包。
- `on_dismiss` 和 `on_snooze` 都应触发返回动画。

### 4. 动作库 / gif 效果动画
用户原话："任务添加一个动作库，能够产生类似 gif 的效果，在本地可以动起来。"
- 扩展现有 `animation_sequence` 机制，让用户能从右键菜单触发"动作"播放（例如"挥手""跳跃""哭泣"等预设动画序列）。
- 在右键菜单增加一个"动作"子菜单或几个动作按钮（至少 3 个：挥手、跳跃、哭泣/撒娇）。
- 每个动作对应一组 frame_index 序列，以 ~120-160ms/帧循环播放若干次后回到 idle。
- 复用现有的 `animation_sequence` + 帧定时器逻辑（见 `wire_notification` 附近的 `animation_sequence` 消费处）。若现有消费逻辑只服务于通知，请把它抽成通用的 `play_animation(sequence: Vec<i32>, loops: usize)` 并在右键动作和提醒处共用。
- 动作定义可放在 model.rs 或一个新的 `animations.rs` 常量表。

### 5. 退出时哭哭确认
用户原话："如果选择退出，呜呜哭哭主人不要退出，提升小宠物趣味性。"
- 当前 `pet.on_exit` 直接退出。改为：点击"退出"时，先播放一个"哭泣/不舍"动画序列（复用第 4 点的动作系统，比如 `[7,7,7,4]` 循环 2 次），并显示一个小通知气泡"呜呜，主人不要走~"，播放完成后再真正退出（或弹一个二次确认 rfd 对话框，文案如"真的要离开鸦影吗？"，用户确认后才退出）。
- 提供 `on_exit` 的两段式：第一次触发播放哭泣动画+气泡，动画结束或用户二次确认后调用 `slint::quit_event_loop()`。

### 6. 右键菜单不盖住宠物
用户原话："右键不要盖住宠物形象。"
- 当前菜单居中叠在桌宠正上方。改为：菜单出现在桌宠**旁边**（例如右侧或左侧，紧贴宠物图像边缘，留 8-12px 间距），并在接近屏幕边缘时自动翻转到另一侧或上/下方（复用 `platform::active_work_area` 做边界 clamp）。
- 菜单位置计算可在 Rust 侧（新增回调把屏幕坐标传给 Slint）或纯 Slint（把菜单锚点改为 `x: parent.width + 8px` 并 clamp）。确保宠物图像始终可见。

## 约束
- 保持现有代码风格（中文注释、模块划分）。不要引入新的运行时依赖除非必要（tray-icon 可加）。
- Windows 特定代码用 `#[cfg(windows)]`，提供 `#[cfg(not(windows))]` 的合理 fallback/stub（便于 CI 在非 Windows 上至少 `cargo check` 通过；注意 CI 是 Windows release 构建）。
- 所有用户可见文案用简体中文。
- 完成后运行 `cargo fmt`、`cargo clippy --all-targets`（可接受仅因 Windows API 在非 Windows 的 cfg 警告，但逻辑代码必须干净）。
- 只修改必要的文件：`Cargo.toml`、`src-rust/*.rs`、`ui/app.slint`、`build.rs`（如需要）、以及图标资源。
- 不要修改 `.github/workflows/rust-release.yml` 或任何认证/密钥配置。

完成后请用中文简要说明你改了哪些文件、每个需求点如何实现、以及是否有任何已知限制。
